# Git Configuration
GIT_USERNAME=your_username
GIT_EMAIL=your_email@example.com
GIT_TOKEN=your_personal_access_token

# Docker Registry Configuration
# When using Docker Compose, this should be set to 'registry:5000' (service name in docker-compose.yml)
# For local development without Docker Compose, use 'localhost:5000'
REGISTRY_URL=localhost:5000
REGISTRY_USERNAME=
REGISTRY_PASSWORD=

# Application Configuration
# This directory will be mounted as a volume in Docker Compose
TEMP_DIR=/tmp/docker-webui
DEFAULT_DOCKERFILE_PATH=Dockerfile
